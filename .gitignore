# Secrets
.env
.env.local
.env.*.local
*.key
*-secret*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (hvis vi senere tilføjer build-tooling)
node_modules/
npm-debug.log*

# Local server caches
.cache/

# Skills installed via `npx skills add` (dev-tooling, not part of deployed artifact)
.agents/
.skills/
skills-lock.json
.impeccable/

# Supabase CLI (hvis vi senere bruger det lokalt)
supabase/.branches/
supabase/.temp/
